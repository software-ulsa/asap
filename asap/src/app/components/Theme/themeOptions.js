import { red } from "@mui/material/colors";

const themeOptions = {
  typography: {
    fontSize: 14,
    body1: { fontSize: "14px" },
  },

  status: { danger: red[500] },
};

export default themeOptions;
